using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainCharacterGearUI : MonoBehaviour
{
    //CharacterWeaponGearUI[] weapons;

    //private void Awake()
    //{
    //    weapons = GetComponentsInChildren<CharacterWeaponGearUI>(true);
    //}
    //public void UpdateWeapon()
    //{
    //    //GameData.Instance.staticData.GetItemData(e.itemId).slot == ItemType.WEAPON
    //    SkillName skillName = SkillName.NONE;
    //    GameData.Instance.playerData.saveData.ListEquipped.ForEach(e => {
    //        EquipmentData equipmentData = GameData.Instance.staticData.GetEquipmentData(e.itemId) as EquipmentData;
    //        if (equipmentData.slot == ItemType.WEAPON)
    //        {
    //            skillName = equipmentData.passiveSkill;
    //        }
    //    });

    //    foreach (var weapon in weapons)
    //    {
    //        foreach (var skill in weapon.WeaponSkills)
    //        {
    //            weapon.gameObject.SetActive(skill == skillName);
    //            break;
    //        }
    //    }
    //}
}
